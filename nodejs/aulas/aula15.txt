estruturas de controle
switch case (opções)
// uso com variáveis do tipo números inteiros
variáveis
let variavel

switch(variavel) {
     case 1:
    // codigo caso o valor da variável seja 1 
    break
     case 2:
    // codigo caso o valor da variável seja 2 
    break
    default:
    // codigo caso nenhum valor inexistente encontrado 
     break 
}



-----------------------------------------------------------------
gerar numeros aleatorios (javascript)
math (recurso para funçoes matematicas)
math.floor (transforma um numero real em um numero inteiro)
math.random() (gerador de numeros aleatorios)
ex: math.floor(math.random() *60)
// sorteio de qualquer numero entre 0 e 59